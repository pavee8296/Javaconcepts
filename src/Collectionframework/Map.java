package Collectionframework;

public class Map {
	
	// When ever we need to store a key value arrangement in the program 
	//Map is an interface 
	
	// The keys should be unquie the values can contain duplicates 

	// since the key is unique it is stored in set 
	
	// To store the key it uses set class & to store the values it uses LIst class 
	
	// hashtable - this is thread safe, only one thread executes at a time multiple thread can not be used at a time 
	
	// hashmap - the execution will be random 
	
	// Treemap - this will provide the sorted represntation of the values 
	
	// Linkedhashmap - when we try to access the elements it will maintain the insertion order 
}
